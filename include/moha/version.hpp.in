#pragma once
// AUTO-GENERATED by CMake from include/moha/version.hpp.in.
// DO NOT EDIT THIS HEADER — bump the version in the top-level
// CMakeLists.txt's project(moha VERSION ...) line; this file
// regenerates from that on the next configure.
//
// Single source of truth for moha's version. Every UI label,
// HTTP user-agent, log line, and crash report should consume the
// constants here — never hard-code a `0.1.0` string anywhere.
//
// Why an .in template + configure_file? Because hand-edited
// version.hpp would drift from CMakeLists.txt the moment someone
// bumped one without the other. The template binds them at
// configure time so the only mutation point is `project(VERSION ...)`.

#include <string_view>

namespace moha {

// ── Numeric components ────────────────────────────────────────────
inline constexpr int kVersionMajor = @PROJECT_VERSION_MAJOR@;
inline constexpr int kVersionMinor = @PROJECT_VERSION_MINOR@;
inline constexpr int kVersionPatch = @PROJECT_VERSION_PATCH@;

// ── String forms ──────────────────────────────────────────────────
// `kVersion`      — full canonical "MAJOR.MINOR.PATCH" (use for
//                   HTTP User-Agent, CLI `--version`, crash logs).
// `kVersionShort` — "MAJOR.MINOR" (use for compact UI labels where
//                   the patch is noise).
// `kUserAgent`    — pre-built "moha/MAJOR.MINOR.PATCH" — the canonical
//                   wire identity, used for every outbound HTTP
//                   request (Anthropic API, web_fetch, web_search,
//                   OAuth exchange).
// `kName`         — the project name, by itself. Useful for window
//                   titles and any place that wants to format its
//                   own "<name> <version>" string.
inline constexpr std::string_view kName         = "moha";
inline constexpr std::string_view kVersion      = "@PROJECT_VERSION@";
inline constexpr std::string_view kVersionShort = "@PROJECT_VERSION_MAJOR@.@PROJECT_VERSION_MINOR@";
inline constexpr std::string_view kUserAgent    = "moha/@PROJECT_VERSION@";

} // namespace moha
